namespace Calendar.Infrastructure.Services.ServiceInjector
{
    public class ServiceInjector
    {
        public static void RegisterInjector()
        {
            //var container = new SimpleInjector.Container();
            //container.Options.DefaultScopedLifestyle = new WebRequestLifestyle();
            //container.Register<IPaymentService, PaymentService>(Lifestyle.Scoped);
            //container.Register<IMonthsService, MonthsService>(Lifestyle.Scoped);

            //container.RegisterMvcControllers(Assembly.GetExecutingAssembly());
            //container.Verify();
            //DependencyResolver.SetResolver(new SimpleInjectorDependencyResolver(container));
        }
    }
}
